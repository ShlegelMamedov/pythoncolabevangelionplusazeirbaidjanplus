# pythoncolabevangelionplusazeirbaidjanplus

1- Преимущества Google Colab:
  Не требует установки ПО — работает в браузере
  Бесплатный доступ к GPU/TPU для сложных вычислений
  Автосохранение в Google Drive + совместная работа
  Предустановленные библиотеки (numpy, pandas, matplotlib)
2. Типы ячеек в Colab:
  Code — для выполнения Python-кода
  Markdown — для оформления текста, заголовков, списков, формул
3. Создание и вызов функции:
def имя_функции(параметры):
    """Описание"""
     код
    return результа т 
    Вызов:
 имя_функции(аргументы)
4. Основные типы данных:
int — целые числа (5, -10)
float — дробные числа (3.14, -0.5)
str — строки ("текст", 'привет')
bool — логические (True, False)
list — списки ([1, 2, "а"])
5. Ввод данных от пользователя:
  input() всегда возвращает строку:
name = input("Введите имя: ")

  Для чисел нужно преобразование:
age = int(input("Возраст: "))
price = float(input("Цена: "))
